// Tom stub for "server-only" når libs kjøres utenfor Next (tsx-scripts).
// Inne i Next håndteres importen av kompilatoren; pakka finnes ikke i
// node_modules på toppnivå. Mappes inn via scripts/tsconfig.json paths.
export {};
